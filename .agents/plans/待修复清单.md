# WBME 待修复清单（阶段 0–10 实现复核）

> 版本：0.6 · 2026-08-10 · 状态：S1–S8 全部已修复并通过验证；M1–M32 全部已修复并通过回归（2026-08-10 批次修复：部署链路/业务硬伤/安全契约/跨服务/前端功能；0.5 独立复核后修复：M12 补 forRoot imports 修复 asset 启动 DI 崩溃、M20/M28 对齐列契约并补 keyword 后端支持、M22 弃 useBlocker 修复利润分析页白屏）；**0.6 独立复核修复（2026-08-10 全量验证后）**：M22 站内离开保护死代码（容器 switch 卸载使 location 检测失效——改为利润分析常驻渲染，测试改容器结构并 portal mock）、S4 迁移钩子天级幂等键与分钟级指纹冲突导致同日重试 409 锁死（改服务端分钟级自动键）、S7 worker 池执行中 terminate 不 reject（补 reject）+ excel-cancel 集成测试缺 loadEnvFile 默认被跳过与 FIN 未 OPEN 致 HTTP 用例假阳性（补加载与置 OPEN）、M18 matchRows `AND: []` 恒真全表读取（空年度行省略分支）、M8 correctBatch 锁序（批次前锁与全系统"条目升序→批次"相悖——改条目升序后锁批次并锁读校验）、S8 批量注销锁序（目标锁前于全超管锁成环——调换顺序）、M2 me 导出补限流 + RateLimit envPrefix 可配置化（PRD §9.7）、M4 停写轮询漏 `idle in transaction` 连接、M5 image_objects 注册表同步清理 + 迁移缺 schema 前缀（表误建于 base，模型声明 backstage——运行时必失败，已修迁移并迁移 dev 库）、OSS_PREFIX 死环境变量（constants 读 env）、M20 三个申请历史页 name 列恒空（REQUEST_COLUMNS 对齐 ApprovalRequest）、M27 岗位申请记录死筛选移除；**M21 复核结论**：asset 审批"导出已筛选"经同一 export/all 端点携带筛选参数，后端 exportList 完整解释——筛选实际生效，无需修复；文档失实 3 处修正（nginx master 仍以 root 运行、recovery-session.client nginx 反代注释、OSS_PREFIX 注释）；补测试：M17 fin 确认幂等重放/409、M7 asset process 幂等重放、M1 profile-change submit 幂等重放；**范围外残留修复（M10 同类）**：fin/hr 共 6 个批量端点（fin `projects/batch`/`deleted/restore`，hr `departments/positions/title-rules/dicts/delete`）补齐幂等键通道（DTO 继承 IdempotentDto + controller 透传 header 注入键 + service 签名接入；hr dict 空指纹修正为 `fingerprintPayload({ids})`），全仓 `idempotencyKey: undefined` 清零，OpenAPI 与 API 文档同步；L1–L38 轻微问题待后续处理（其中 M4 停写轮询成功/超时路径、M13 P2002 并发兜底两条测试缺口保留）
> **0.7 独立复核修复（2026-08-10，逐项代码核实 + 全量测试验证）**：**S4 阻断级**——migration-runner 生产运行时镜像无 pnpm 且根 workspace 清单未复制，`pnpm --filter` 迁移命令必 ENOENT（任何发布必失败）；改直接调用各 app `node_modules/.bin/prisma`（cwd 定位 prisma.config.ts），真实试跑 4 单元迁移全过；**S7 阻断级**——worker 池 spawn 内 pending Map 从未写入，生产线程模式任务完成消息永不 resolve（导入/导出挂起至路由超时，仅测试 inline 模式掩盖）；改经 this.pendingTasks 跟踪，真实 worker 线程冒烟验证完成；M22 常驻渲染后 leaveConfirmedRef 永不复位（确认一次"放弃并离开"后站内保护永久失效）——onOk 清空草稿并复位，补"再次编辑再次离开仍弹确认"回归测试；M8 transfer 锁序残留（目标条目在批次锁后获取，反向调拨/与 correctBatch 交叉可死锁）——目标条目并入来源升序锁定，清理 findOrCreateItem 死代码；M12 asset-department.client 不检查 response.ok（asset 错误时置空静默失败、hr 删除照常提交）——非 2xx 抛错/降级；S8 并发注销同一用户无条件重写撞 task_uuid 唯一约束 500——改 lifecycleVersion 条件更新输出业务冲突；M13 并发同 rid 无交集目标集双双成功——事务级 advisory lock（hashtext rid）串行化，实测 PG 验证；M5 注册表清理先删对象后删注册表失败留孤儿行——调换顺序并补断言测试；S2 版本校验零直接测试——补 backup.processor.spec 3 例；S7 终末检查点（审计后提交前补 checkTimeout）+ worker 池 dispatch 跳过终止中 worker；M32 演练文档 Cookie 须在停止服务前签发（步骤 1.5 前置）；M4 清单措辞修正（中止后保持维护状态）；M2 RATE_LIMIT 变量进 env 示例；backup.controller 注释修正；M17 集成测试用例自清理；dev 库测试残留已清
> **0.8 复核修复（2026-08-10，真实 PG 并发实测复现后修复）**：S5 correctBatch 并发丢失更新——锁读一致性校验仅比 identity 三字段、供应商/品牌/单价/备注兜底取过期 peek，并发「改规格」与「改供应商」交错时后提交者以过期值覆盖先提交者修改且审计 before 失真；改兜底字段一律取锁后 batchRow/sourceItem，来源条目锁前/锁后缺失时区分「批次仍在但归属被并发移动」（VALIDATION_FAILED 重试）与「批次消失」；M8 反向调拨/反向纠正 40P01——先单锁来源再补锁目标，A→B 与 B→A 并发成环死锁；改无锁定位来源+目标后一次性按 id 升序锁定（transfer/correctBatch），stock-in applyApproved 改整单无锁批量定位已存在条目后一次性升序锁、不存在条目锁后创建；M22 离开保护两竞态——保存请求在途（saving）时导航离开无提示（saving 纳入未保存判定与 beforeunload）、确认放弃后在途保存失败重新置 dirtyRef 致离开后二次拦截（abandonedAtRef 放弃时刻，放弃前发起的失败不再标记），清理 leaveConfirmedRef 死代码；**新发现阻断级**——恢复状态目录属主缺口：release.sh 不预建 /opt/wbme/persist/restore-state，Docker bind mount 自动建 root:root，recovery-executor 以 uid 10001 运行致 readyz 写探针 EACCES、首次发布第 5 节必超时 fail；release.sh 部署前置 mkdir + chown 10001:10001，restore-drill.md 补手工部署说明。验证：真实库并发 15 轮（双双成功时供应商恒为新值，丢失更新消失）、反向调拨/交叉并发 10 轮无 40P01；M22 补「saving 导航仍弹确认」「放弃后在途失败不再二次拦截」两回归用例；全量测试/typecheck/lint 全绿
> **0.9 独立复核修复（2026-08-10，代码核实 + 真实 PG 复现 + 全量验证）**：**M2 确证无效并修复**——RateLimitGuard 从未注册为 provider，Nest 11 对未注册守卫类静默跳过（getGuardInstance 返回 null 被 filter），platform-core 11 处限流与**登录/激活/注册/密码/钉钉认证类端点限流全部未生效**；已在 @Global SharedModule 注册并导出（REDIS_CLIENT 由全局 RedisModule.forRoot 提供），新增 `rate-limit.integration.spec.ts`（真实 SharedModule + 真实 Redis + 真实 Nest app：第 3 次请求 429 RATE_LIMITED；守卫被移出 providers 时测试即失败）；**M9 确证无效并修复**——本系统注销即置 deleted_at（user-lifecycle.service 注销同步写 deletedAt、恢复才清除），仅去 status='ACTIVE' 过滤对已注销用户效果不变；COMPANY 档改为不过滤任何用户状态（无物理删除语义），与 DEPARTMENT 档口径一致，PRD §2.6 文字已先行同步；**M8 残留修复**——borrow `restoreRecord` 先锁批次（FOR UPDATE OF ba, b）再锁条目，与调拨「条目升序→批次」反向成环；改无锁定位分配行 → 先锁条目 → 锁后重读分配+批次（最终校验以锁后为准），确定性交错复现旧序 deadlock detected、新序无死锁；**S5/S8 测试固化**——inventory.service.spec 新增 5 轮并发交错用例（改供应商 vs 改规格：拒绝仅允许 VALIDATION_FAILED、至少一笔成功、供应商修改必须保留——丢失更新断言），修测试清理按 batch_id 清流水（cleanupEmptyItem 删空条目后按条目过滤会漏流水致 FORBIDDEN 误报）；S8 并发用例补注释说明 UNAUTHORIZED 为合法保护路径（操作人被并发注销）、核心护栏为 remaining=1 断言；**M10 范围外残留**——fin dict 批量删除补齐幂等键通道（FinDictItemBatchDeleteDto extends IdempotentDto + controller 透传 + service 接入 runDictWrite），全仓 `idempotencyKey: undefined` 清零；验证：全量测试 526 用例/typecheck/lint 全绿，临时数据已清
> 依据：阶段 0–10 全部代码实现与 PRD（主 PRD + base/backstage/asset/hr/fin）对照审查结论；0.3 版为 2026-08-10 复核（8 项严重问题逐项验证 + 中等问题逐项代码核实）结论。
> 修复完成后逐项勾选，并在 `实现规划.md` 中同步状态。
> 涉及产品口径的项（标注「需产品确认」）须先与产品确认再实现，确认结论同步回对应 PRD。
>
> 0.3 复核结论摘要：S2（pg_dump 版本）与 S4（迁移前备份）在 f9e2c90 中的修复均无效——S2 的 Dockerfile 镜像源路径 404（构建必失败），S4 未解决"迁移先于平台服务启动"的时序矛盾（备份端点不可达，迁移必中止）；两处需按本版标注的修正方向重做。M1–M32 共 32 项经代码核实全部真实存在（28 确认 + 4 部分成立，0 误判），标题数量由"21 项"更正为"32 项"（前端实际 9 项）。

## 约定

- 编号：`S` 严重（必然触发/阻断验收/安全底线）· `M` 中等（明确违背 PRD 契约）· `L` 轻微（体验、注释、声明漂移、健壮性）。
- 子系统前缀：`PC` platform-core（含 worker/recovery-executor）· `AS` asset · `HR` hr · `FIN` fin · `WEB` 前端 · `DEP` 部署收尾。
- 每项修复完成后须跑相关单测与聚合检查（`pnpm lint / typecheck / test / build`）；涉及 OpenAPI 契约的同步 `openapi:generate`；涉及接口的同步 API 文档与 `docs/directory.md`（如结构变化）。

---

## 一、严重问题（8 项）

### ✅ S1【DEP】生产发布必然失败：恢复执行器探针路径错误
- 位置：`deploy/docker-compose.yml:270`（healthcheck）、`deploy/release.sh:101`（wait_ready）、`deploy/restore-drill.md:19-20`
- 问题：恢复执行器就绪探针真实路由为 `/recovery/readyz`（`apps/recovery-executor/src/recovery-control.controller.ts:10,26`，`@Controller('recovery')`），compose healthcheck 与 release.sh 均探测 `:3090/readyz` → 404 → 容器永久 unhealthy、发布第 5 步等待超时 fail。任何发布（含无迁移常规发布）必失败。
- 修复方案：
  1. compose healthcheck 探针路径改为 `/recovery/readyz`；
  2. release.sh 的 `wait_ready` 支持路径参数，恢复执行器使用 `/recovery/readyz`；
  3. `restore-drill.md` 前置检查同步修正。

### ✅ S2【DEP】备份/恢复在生产完全不可用：pg_dump 15 vs PostgreSQL 18（0.3 复核重做完成，2026-08-10）
- 位置：`deploy/Dockerfile:43`（`postgresql-client-15`）、`deploy/docker-compose.yml:57`（`postgres:18-alpine`）
- 问题：pg_dump 15 拒绝导出比自身新的大版本服务器（`server version 18.x; pg_dump version 15.x`）→ 定时备份、立即备份、迁移前备份、恢复管道全部硬失败。
- 修复方案：
  1. runtime 镜像改用 PostgreSQL 18 客户端：推荐以 `postgres:18-alpine` 为 runtime 基础镜像（自带 pg_dump/pg_restore 18）再装 Node 环境；或 apt.postgresql.org（pgdg）源安装 `postgresql-client-18`；
  2. 备份处理器增加"客户端版本 ≥ 服务器版本"启动校验，防再次漂移。
- **0.3 复核重做（2026-08-10）**：f9e2c90 的版本校验（backup.processor.ts `assertPgClientCompatible`）保留；Dockerfile 镜像源已从清华（实测 404）改为阿里云 `https://mirrors.aliyun.com/postgresql/repos/apt`（密钥同路径 `/ACCC4CF8.asc`，2026-08-10 实测：Release/Packages/密钥均 200，`postgresql-client-18`（18.4-1.pgdg12+1）存在，依赖 `postgresql-client-common ≥182` 提供 `/usr/bin/pg_dump` alternatives 链接，与 compose 的 `PG_DUMP_PATH=/usr/bin/pg_dump` 一致）。验证：worker backup-task/index 测试 6/6 通过。留待 ECS 构建时最终确认。

### ✅ S3【DEP】生产 OSS 存储链路断裂：RAM 角色声明与 AK/SK 实现矛盾（0.3 复核修复完成，2026-08-10）
- 位置：`deploy/.env.production.example:35`（注释"生产使用 ECS 实例 RAM 角色（不填 AK/SK）"）、`packages/files/src/oss-config.ts:18-40`（仅读 `OSS_ACCESS_KEY_ID/SECRET`）、compose 未注入 OSS 凭证
- 问题：生产不填 AK/SK → `createOssClient` 返回 null → 静默回退 `LocalFileStorage`（根 `.agents/tmp-oss`），生产容器 `read_only: true` 且未挂载该目录 → EROFS 写入失败。图片上传、备份对象上传、恢复取备份全链不可用。与项目既定决策（ECS RAM 角色，见记忆 `wbme-oss-production-ram-role`）矛盾。
- 修复方案（二选一，推荐 a）：
  a. `oss-config.ts` 支持 ECS RAM 角色（阿里云 SDK 实例元数据/STS 凭证，无 AK/SK 时启用）；
  b. 决策改为 AK/SK 注入（更新模板、compose、记忆与部署文档）。
  同时：生产模式（`NODE_ENV=production`）下禁止静默回退 LocalFileStorage，缺凭证直接抛配置错误。
- **0.3 复核修复完成（2026-08-10）**——`oss-config.ts` 已支持 ECS RAM 角色（实例元数据 + STS 自动刷新）、生产禁静默回退已生效，`createFileStorage` 全仓调用点均已适配 async。本次收尾：
  1. compose 四个 OSS 使用服务（platform-core/asset/worker/recovery-executor）统一注入 `OSS_RAM_ROLE_NAME: ${OSS_RAM_ROLE_NAME:-}`（多角色实例可显式指定角色名；单角色自动取元数据第一个角色名，填不填均可）；
  2. **额外发现并修复**：worker 此前完全没有 OSS 环境变量注入（`OSS_BUCKET/OSS_REGION/OSS_PREFIX_IMAGES/OSS_PREFIX_BACKUPS` 均缺）——S3 禁静默回退后生产 worker 执行备份上传/图片清理必然抛"生产环境 OSS 未配置"，备份链路在 S3 修复后依然断裂；本次已补齐 worker 五组 OSS 变量。
  验证：compose YAML 解析校验通过（hr/fin/migration-runner/web 无需 OSS，未注入符合预期）。

### ✅ S4【PC】有迁移的发布与首次部署必然失败：迁移前备份钩子 401（0.3 复核重做完成；0.6 复核修复：钩子天级幂等键与分钟级指纹冲突 → 同日重试 409 锁死，改服务端分钟级自动键）
- 位置：`apps/migration-runner/src/pre-migration-backup.hook.ts:45-53`（Bearer 令牌调用）、`apps/platform-core/src/modules/backstage/backup/backup.controller.ts:22-23,35`（SessionGuard + FunctionPermissionGuard）、`packages/server/src/session/session.guard.ts:65-70`（只认会话 Cookie）、`deploy/docker-compose.yml:114`（`PRE_MIGRATION_BACKUP_WAIT: "1"` 已启用）
- 问题：钩子用 `Authorization: Bearer` 调 `POST /api/v1/backups/immediate`，该端点只认会话 Cookie + `data_backup` 权限 → 401 → runner 中止迁移；全新库场景 platform-core 尚未启动 → 首次部署死锁。单测仅用 mock 覆盖 env 分支。
- 修复方案：
  1. 新增 `/internal/v1/backups/immediate` 内部端点（InternalTokenGuard + 调用方白名单，保留备份互斥锁与"存在未完成恢复清单则拒绝"逻辑）；
  2. 钩子改调内部端点；`PRE_MIGRATION_BACKUP_CMD` 保留为逃生通道；
  3. 补真实 HTTP 路径集成测试。
- **0.3 复核：修复无效**——内部端点/守卫/白名单/钩子改造/集成测试（4/4 通过）均正确，但**调用时序未解决**：release.sh 在 `up -d` 之前 `docker compose run --rm migration-runner`，而 migration-runner 的 depends_on 只有 postgres/redis。首次部署（空库）platform-core 未启动 → 连接拒绝；升级部署旧版 platform-core 无内部端点 → 404。**两种情况迁移前备份都必然失败 → 迁移中止 → 发布失败**。且 readyz 含迁移版本检查（`packages/server/src/health/migration-readiness.ts`），迁移前平台服务恒 503，"先起平台服务等 healthy 再迁移"不可行。
- **重做完成（2026-08-10）**：
  1. `release.sh`：有发布基线（升级部署）时先 `up -d platform-core worker` → 以 `healthz`（免迁移检查的进程探针，`wait_ready` 已支持路径参数）与 `wait_worker_healthy` 判定可用 → 再 `run --rm migration-runner`（备份→迁移）→ 最后 `up -d` 全部；`wait_ready`/`wait_worker_healthy` 函数定义提前到第 4 节前共用。readyz 含迁移版本检查（`packages/server/src/health/migration-readiness.ts`，迁移前恒 503），故用 healthz 判定进程可用，迁移完成后第 5 节仍以 readyz 复核；
  2. `pre-migration-backup.hook.ts`：新增"空库豁免"——`triggerImmediateBackup` 失败时经 `isFreshDatabase`（pg 直连 `DATABASE_URL` 查 5 个业务 schema 表数，`isFreshDatabase` 可注入便于测试）判定：全新库（无业务表，无数据可备份）则跳过备份继续迁移，否则照常中止；覆盖"手动 `up -d` 首次部署"等不经 release.sh 的路径。
  验证：钩子单测 8/8（新增"触发失败+非空库→中止"与"触发失败+空库→跳过"两例）、worker backup-task/index 6/6、migration-runner typecheck/lint 通过；空库判定 SQL 在真实库实测（96 表→false、临时空库→true，临时库已清理）；release.sh `bash -n` 通过。compose migration-runner 注释已同步时序语义。

### ✅ S5【AS】asset 批次资料纠正接口必然 500
- 位置：`apps/asset/src/modules/inventory/inventory.service.ts:282`（`$queryRaw` 选择 `b.warehouse_id`）
- 问题：`asset.batches` 表不存在 `warehouse_id` 列（Prisma 模型与迁移均确认：批次只有 `warehouse_name/path` 快照；`warehouse_id` 仅存在于 `inventory_items`/`stock_in_items`）→ 任何纠正请求 PostgreSQL 42703 → 统一 500。T7-4 验收"纠正不绕过审批"对应 API 100% 不可用；asset 侧 3 个 spec（asset-approval/gate-regression/inventory-core）无一触达 correctBatch。
- 修复方案：
  1. 纠正 SQL 与读取结果类型中移除 `warehouse_id`；
  2. 补 `correctBatch` 单测（正常纠正 + 无后续流水限制 + 占用限制）。

### ✅ S6【WEB】HR 部门管理"新建部门"必然 404
- 位置：`apps/web/src/pages/hr/HrPage.tsx:53`（`create` 未配置 endpoint）vs `apps/hr/src/modules/org/department.controller.ts:22,29`（`@Get('tree')` / `@Post()`）
- 问题：`ResourcePage` 默认用列表端点 `/departments/tree` 提交 POST → 404。asset 库位管理显式配置 `endpoint: '/warehouses'` 规避了同类问题。
- 修复方案：HrPage 部门 `create` 配置 `endpoint: '/departments'`；顺带检查其它未显式配置 endpoint 的 create 项（岗位/职称/其余 ResourcePage 实例）。

### ✅ S7【FIN】fin Excel 导入取消/超时传播不彻底：确认事务可在超时响应后提交（0.3 复核补测完成；0.6 复核修复：worker 池执行中 terminate 不 reject 致取消窗口挂起至路由超时——补 reject；集成测试缺 loadEnvFile 默认被跳过 + dev 库 FIN 未 OPEN 致 HTTP 用例 3/4 假阳性——补加载与置 OPEN）
- 位置：`apps/fin/src/modules/excel/excel.controller.ts:87-94`（abortSignal，已改 `res.on('close')`）、`packages/server/src/request-timeout.interceptor.ts:23-27`（rxjs timeout 仅停响应）、`apps/fin/src/modules/excel/import.service.ts:654-718`（applyImport 尾部无检查点）
- 问题：**已完成部分**——取消信号源已修复（`req.on('close')` → `res.on('close')`；`makeImportTimeoutCheck` 已接入 signal 检查；worker 池已支持排队丢弃 + 执行中 terminate；新增/覆盖循环已补检查点）。**剩余缺口**：路由级 150s 超时只停止监听响应，handler 继续执行；`applyImport` 在覆盖循环最后一次 `checkTimeout()`（import.service.ts:654）之后，审计 createMany（671-709）与事务提交之间再无检查点，**超时/取消在此窗口触发时，事务仍会继续完成并提交**——违反 fin PRD §4"不能在超时响应后继续后台写入"。
- 修复方案：
  1. `applyImport` 在审计 createMany 前与事务返回前各加一次 `checkTimeout()`；
  2. 补控制器层真实 HTTP 取消测试（上传中断/响应关闭/路由超时三种场景），验证超时后事务回滚、无部分写入。
- **0.3 复核补测完成（2026-08-10）**：`abortSignal`（响应侧 `res.on('close')`）、`makeImportTimeoutCheck` 接入 signal、applyImport 4 处检查点均确认有效。新增 `excel-cancel.integration.spec.ts`（真实 Prisma/Redis/worker 池 + 真实 Nest app，4 用例全过）：
  1. service 层调用前已取消 → 首个检查点抛错、无任何写入；
  2. service 层写入窗口内取消（2000 行文件 + 800ms 定时 abort）→ 检查点抛错、事务回滚、无部分写入；
  3. HTTP 层上传中断（请求体发送一半断开）→ handler 不执行、无写入；
  4. HTTP 层响应关闭（完整发送后客户端断开 → res 'close' → abort）→ 无任何写入。
  断言覆盖：覆盖目标 dataRevision 不变、无新增行、无审计残留；afterAll 清理（先删审计再删项目）后实测无残留。excel 模块全量 28/28 通过。路由超时场景（150s 路由级常量无法短时复现）由 interceptor 的 rxjs timeout → 响应关闭 → abort 链路与场景 2/4 共同保证。

### ✅ S8【PC】批量注销可清空全部超级管理员（并发竞态）
- 位置：`apps/platform-core/src/modules/backstage/user-admin/user-lifecycle.service.ts:366`（无锁 count）vs `super-admin.service.ts:123-127`（`SELECT ... FOR UPDATE` 锁全部超管行）
- 问题：批量注销只锁批内目标行，再对全部超管做无锁 count。两个并发批次分别注销两名不同超管时双双通过 → 0 名可用超管，平台失去根管理员。
- 修复方案：`loadDeactivationTargets` 内可用超管计数改为 `SELECT id ... FOR UPDATE`（与 demote 一致），锁内重新计数；补并发集成测试。

---

## 二、中等问题（32 项）

### platform-core（6 项）

**✅ M1【PC】审批/资料修改提交/批准/驳回/取消的幂等键被接受但忽略**
- 位置：`apps/platform-core/src/modules/base/approval-proxy/approval.controller.ts:68,79`、`me.controller.ts:20,97`、`profile-change.service.ts:34-217`；DTO `packages/contracts/src/dto/approval.dto.ts:72,92`（声明 IdempotentDto）
- 问题：状态机/版本条件更新/部分唯一索引可防重复副作用，但同键重试返回 409/`PROFILE_CHANGE_PENDING_EXISTS` 而非原结果，契约违背；`me.controller.ts:19` 注释"幂等键防重复提交建单"与实际不符。
- 修复方案：三个方法增加 `idempotencyKey` 参数并接入 `executeIdempotentOperation`（operation-log.util 已有现成机制），controller 透传 `dto.idempotencyKey`。

**✅ M2【PC】批量操作与导出接口全部零限流**（0.6 复核修复：`/me/operation-logs/export` 补限流；RateLimit 增加 envPrefix 使 limit/windowSeconds 可由环境变量配置，满足 PRD §9.7 可配置限流；**0.9 复核确证 0.6/0.8 修复无效并修复**——RateLimitGuard 从未注册为 provider，Nest 11 对未注册守卫静默跳过（不报错），11 处限流与登录/激活/注册/密码/钉钉认证类限流全部未生效；已注册进 @Global SharedModule 并补 RATE_LIMITED 集成测试）
- 位置：user-admin.controller.ts:58-59,70、permission.controller.ts:45-53、announcement 批量删除、backup.controller.ts:35,50（立即备份/恢复确认）、operation-log/system-log/approval 导出端点；全仓 `RateLimit` 仅存在于认证类端点
- 问题：导出仅有单用户并发互斥锁（EXPORT_ALREADY_RUNNING），无频率限制。主 PRD §9.7 明确要求"导出、批量操作…设置可配置限流"。
- 修复方案：为批量操作类端点按用户/IP 加 `RateLimit`（scope 按端点语义）；导出建议加每用户每小时导出次数上限。

**✅ M3【PC】公告批量删除静默跳过不存在目标**
- 位置：`apps/platform-core/src/modules/backstage/content/announcement.service.ts:199-209`（updateMany 199-202、返回 `deleted: ids.length` 在 204）
- 问题：不存在/已软删目标被静默跳过，返回值按入参 ids 计数。违背主 PRD §2.6"整批不变更 + 逐项目标原因"。
- 修复方案：事务内先 `findMany` 校验数量与 ids 一致，不一致抛校验错误附缺失 id 明细；全部存在后批量软删，返回实际删除数。

**✅ M4【PC】恢复执行器停写确认缺失**（0.6 复核修复：轮询条件补 `idle in transaction` 连接——该状态连接 xact_start 非空仍可写入；轮询成功/超时路径测试缺口保留）
- 位置：`apps/recovery-executor/src/recovery-executor.service.ts:356-362`（`WRITE_DRAIN_WAIT_MS = 5_000` 定义于 :54）
- 问题：仅固定 sleep 5 秒，无 `pg_stat_activity` 确认存量写连接排空。backstage PRD §10 要求"确认除恢复执行器外不存在仍可写入目标数据库的连接"。
- 修复方案：改为轮询 `pg_stat_activity`（排除自身/复制连接，等待活跃写事务为 0 或超时上限）；超时则记录仍活跃连接明细并中止恢复、**保持**维护状态（0.7 复核修正表述：中止恢复后维护标记保留、错误落盘供人工核查，而非退出维护状态——代码行为才是安全正确的）。

**✅ M5【PC】图片下载接口无业务授权/归属校验**（0.3 复核：CONFIRMED，建议首选方案需修正；0.6 复核修复：image-cleanup 删对象同步清注册表行；迁移 SQL 缺 backstage schema 前缀致表误建于 base、模型访问 backstage 运行时必失败——已修迁移并迁移 dev 库；「业务引用授权」（下载者≠上传者共享场景）为 MVP 取舍，当前 web 无调用方）
- 位置：`apps/platform-core/src/modules/files/images.controller.ts:29-33`（无 `@CurrentUser`）、`images.service.ts:67-75`（仅正则校验键格式）
- 问题：任何登录用户凭对象键可下载任意正式图片；`packages/files/src/file-storage.ts:84-96` finalizeImage 缺省复用 pending 键，临时/正式对象键结构相同，"临时对象不提供下载通道"无机制强制。主 PRD §9.2 要求"只有后端处理后的私有正式对象可被业务记录引用和限时下载"。
- 修复方案：downloadUrl 增加归属校验（键内 userId 与当前用户比对、超管豁免）；维护 finalize 对象注册表供下载校验；或业务引用图片走业务模块内嵌签名接口。
- **0.3 核实**：问题确认（`images.controller.ts:29-33` 无 `@CurrentUser`、`file-storage.ts:90` finalize 缺省复用 pending 键，任意登录用户可凭对象键取任意图片签名 URL）。**建议修正**：首选"键内 userId 比对"与 PRD §9.2"正式对象被业务记录引用和限时下载"语义冲突（下载者≠上传者的共享场景会被误伤），应优先采用"finalize 注册表区分临时/正式 + 业务引用授权校验"（选项 2/3 组合），或在选项 1 中为"已被业务记录引用"的对象增加豁免。当前 web 侧无该接口调用方，属前瞻性风险。

**✅ M6【PC】`me()` 返回的 functionCodes 未按权限目录过滤**
- 位置：`apps/platform-core/src/modules/base/auth/auth.service.ts:146-152` vs `portal.service.ts:41-54`
- 问题：两处口径不一致；已移除功能仍驱动前端显隐（点击后 500）。主 PRD §3.1"移除的功能不再参与入口、菜单或守卫判断"。
- 修复方案：me() 的 functionCodes 经 function 表过滤后返回，与 portal 及 FunctionPermissionGuard 语义对齐。

### asset（5 项）

**✅ M7【AS】审批批准/驳回/取消忽略幂等键**（0.3 复核：PARTIAL——问题成立，位置引用需更正）
- 位置：`packages/contracts/src/dto/approval.dto.ts:72,92`、`apps/asset/src/modules/approval/approval.controller.ts:80-105`、`asset-approval.service.ts:192-313`（签名无幂等参数、无 executeIdempotentOperation）
- 问题：与同服务族 submit 路径（stock-in/claim 均走幂等）反差；乐观锁防重但不重放原结果。主 PRD §3.2 要求"同一幂等键的重试返回原结果"。
- 修复方案：process/cancel 接入 `executeIdempotentOperation`（scope `asset.approval.process/:id`、`asset.approval.cancel/:id`，fingerprint 含 action+opinion），controller 透传键。
- **0.3 核实**：`approval.dto.ts:72,92` 的 `ProcessApprovalDto`/`CancelApprovalDto` **已继承 `IdempotentDto`**（该位置引用不成立）；缺的是 controller（:84-86/:101-103）不透传键与 service 签名无幂等参数——问题实质成立，修复方案不变（仅位置描述修正为 controller/service 两处）。

**✅ M8【AS】调拨/入库批准目标条目不加锁，流水 book_before/after 并发失真**（0.3 复核：PARTIAL——问题成立，两处引用需更正；0.6 复核修复：correctBatch 锁序「批次→条目」与全系统「条目升序→批次」相悖可死锁——改无锁读定位→条目升序锁→锁批次行并锁读一致性校验；**0.9 复核修复 borrow 残留锁序**——`restoreRecord` 先锁批次再锁条目与调拨成环，改无锁定位分配行→先锁条目→锁后重读分配+批次，确定性交错复现旧序 deadlock detected、新序无死锁）
- 位置：`transfer.service.ts:66`（来源锁）、`83-89`（目标 findOrCreateItem 无锁）、`187-198`；`stock-in.service.ts:101-107,140-146`；`inventory-core.ts:226-267`（findFirst+create 无 FOR UPDATE）
- 问题：申领/变更/归还路径全部先加锁再读写账面，仅调拨目标与入库批准未锁；increment 原子不丢更新，但流水 before/after 与并发实际账面漂移，审计链断裂。主 PRD §11"每条流水保存各自变动前后数量"。
- 修复方案：inventory-core 增加 `lockOrCreateItem(tx, input)`（先 `SELECT ... FOR UPDATE`，不存在则 create + P2002 重试后再锁读），调拨与入库批准改用它；`bookBefore` 一律取锁后行值。
- **0.3 核实**：①"主 PRD §11"引用错误——§11 为非功能需求，条款出处为 **asset PRD §6**；②遗漏同类无锁点：**批次纠错**（`inventory.service.ts:352` 的 `findOrCreateItem` 与 `loadItemRow` :463-470 亦非锁读），调用点共 3 处（transfer:83、stock-in:101、inventory:352）应一并迁移到 `lockOrCreateItem`。

**✅ M9【AS】已注销员工历史记录从"范围历史"消失（COMPANY 档）**（**0.9 复核确证 2026-08-10 批次修复无效并修复**——注销即置 deleted_at（user-lifecycle 同步写、恢复才清除），仅去 status='ACTIVE' 过滤对已注销用户效果不变；COMPANY 档改为不过滤任何用户状态，与 DEPARTMENT 档口径一致；PRD §2.6 文字已先行同步）
- 位置：`apps/asset/src/shared/scope-resolver.ts:30-33`（COMPANY 分支 `status = 'ACTIVE'` 过滤）；使用方 stock-in/stock-change/claim/agent-claim controller 均按 applicantId in 过滤
- 问题：COMPANY 档将 DEACTIVATED 员工过滤，其作为申请人的历史申请整条不可见（DEPARTMENT 档经 hr.user_org 不过滤用户状态、不受影响；审批中心与借还历史按快照不受影响，三处口径不一致）。主 PRD §2.6"既有业务记录必须返回并展示该用户原 ID、姓名等快照及已注销标记"。
- 修复方案：COMPANY 分支去掉 `status='ACTIVE'`（保留 deleted_at 过滤），使历史与审批中心/借还历史一致。**需产品确认**"范围历史是否含已注销员工"，结论同步主 PRD §2.6 表述。

**✅ M10【AS】大量重要写接口无幂等键（含批量删除）**（0.3 复核：PARTIAL——问题成立，计数与通道细节需更正）
- 位置：`packages/contracts/src/dto/fixed-asset.dto.ts:113,181,202,209`（AssetUpdate/Schedule/Scrap/BatchDelete 均不继承 IdempotentDto）；服务侧 `idempotencyKey: undefined` 共 16 处：asset.service.ts:351,434,515,564、consumable.service.ts:253,327、warehouse.service.ts:108,148、dict.service.ts:150,190、category.service.ts:129,172、repair.service.ts:137,193,252、qr.service.ts:56,155、inventory.service.ts:253
- 问题：批量软删除网络重试返回 `RESOURCE_NOT_FOUND` 而非首次成功结果；重复请求产生无唯一约束的重复日志行。主 PRD §9.5 明确"批量操作"必须支持幂等键。
- 修复方案：各 DTO 补 `extends IdempotentDto`，controller/服务透传 `dto.idempotencyKey`（服务已接收 dto 处直接透传）。
- **0.3 核实**：①计数错误——`idempotencyKey: undefined` 实际 **18 处**（行号全部吻合，4+2+2+2+2+3+2+1）；②`ConsumableCreateDto`（consumable.dto.ts:30）同样未继承 IdempotentDto，create 幂等键恒 undefined（同类问题未列）；③**repair cancel（repair.service.ts:137，`POST /:id/cancel` 无 body DTO）无法直接"补 DTO extends"**，需新增 body 或 query 参数通道。

**✅ M11【AS】asset 直连 hr 业务表 `hr.departments`**
- 位置：`apps/asset/src/modules/asset/asset.service.ts:457-459,712-715`
- 问题：`hr.departments` 是业务表（H-2，物理删除、name 可改）；`scripts/db-views/` 8 个视图无 departments 视图。主 PRD §9.4 要求跨模块读经只读视图。
- 修复方案：`scripts/db-views/` 增补 `hr.departments` 只读视图（或复用 user_org 的 department_name），asset 两处改经视图读取。

### hr（3 项）

**✅ M12【HR】部门删除的资产联动完全缺失**（0.5 复核修复：①原修复引入启动级回归——`InternalDepartmentController` 注册进无 imports 的 `AssetModule`，`InternalAuthGuard` 的 `INTERNAL_AUTH_OPTIONS` 无法解析，asset 服务启动即 DI 崩溃；已为 AssetModule 补 `InternalRestModule.forRoot({ token })` imports（与 ApprovalModule 同模式），并新增完整模块装配冒烟测试 `asset-module-di.spec.ts` 防止复发。②置空调用时序已调整：`clearAssignments` 从 deleteMany 之前移至事务最后一个语句——之前任何本地步骤失败时置空未执行、事务回滚，两侧一致；残余窗口仅剩"置空成功但提交瞬间失败"（跨服务原子性物理极限，概率极低，后果为部门仍在但资产归属被清空，可人工修正；完全消除需补偿机制，MVP 取舍暂不引入））
- 位置：`apps/hr/src/modules/org/department.service.ts:178-209`（203 行 `assetCount: 0` 占位）、`docs/api-documentations/hr.md:60`（"阶段7接入，当前0占位"）；`apps/asset/prisma/asset.prisma:301`（`Asset.departmentId Int?` 无外键软引用）；asset 无内部计数/置空接口
- 问题：删除预览资产数恒 0、删除事务不置空资产部门归属。hr PRD §6 明文要求"固定资产归属数"与"确认后置空固定资产的所属部门"。
- 修复方案：asset 新增内部接口（如 `internal/v1/departments/{id}/count` 与 `/clear-assignments`），hr 的 deletePreview/deleteBatch 事务内调用；同步更新 hr.md:60 占位说明。

**✅ M13【HR】restore-apply 幂等无唯一约束兜底**（0.3 复核：CONFIRMED，修复方案需修正两处）
- 位置：`apps/hr/src/modules/lifecycle/lifecycle.service.ts:73-88`（事务外先查后写 TOCTOU）；`hr.prisma:251-260`（OrgCompatRecord 无 @@unique）；迁移仅 pkey
- 问题：并发同键异目标集双双成功，`RESTORE_TARGET_STALE`(409) 契约失效。主 PRD §3.3/§9.5 要求幂等以唯一约束为兜底。
- 修复方案：`org_compat_records` 加 `(restore_request_id, user_id)` 唯一约束（或 restore_request_id），事务内捕获 P2002 映射为 `RESTORE_TARGET_STALE`；幂等判定移入事务。
- **0.3 核实**：问题确认（TOCTOU 属实、迁移仅 pkey、并发下 409 契约失效）。**建议修正**：①"或 restore_request_id"选项错误——一次恢复请求可含最多 100 个用户（§9.5），单列唯一会禁止合法的多用户恢复，约束必须是 `(restore_request_id, user_id)`；②"捕获 P2002 一律映射 409"不完整——同 rid 同目标集的并发重放同样触发 P2002，此时应重放成功；正确流程为事务内插入失败捕获 P2002 后回读已提交记录、比对目标集再决定重放或 409。

**✅ M14【HR】worker 审批超时扫描直写 hr/backstage 业务表**
- 位置：`apps/worker/src/processors/approval-timeout.processor.ts:27` → `packages/approval/src/timeout.ts:63-72,101-128`（模板 SQL 直写三 schema 的 approval_requests）；对比 account-lifecycle.processor.ts:26 走 hr 内部 REST
- 问题：双路径并存；主 PRD §9.4 跨 schema 例外仅限任务事实表，未获书面豁免。
- 修复方案：保持直写（schema 白名单 + 条件更新 + 同事务 hook 工程上可接受）则在主 PRD §9.4/§3.2 补充书面豁免声明；否则将 hr/asset 取消路径收敛为内部 REST 统一入口。

### fin（5 项）

**✅ M15【FIN】整表单编辑/批量恢复不递增 dataRevision**（0.3 复核：CONFIRMED，计数与收敛点需注意）
- 位置：`apps/fin/src/modules/project/project.service.ts:156-158`（update）、`252-254`（batchRestore updateMany）；全仓递增仅 2 处（detail.service.ts:248 明细变更封装、import.service.ts:657 导入覆盖条件更新）
- 问题：预览导入 → 表单编辑 → 确认导入，dataRevision 不变 → `IMPORT_PREVIEW_STALE` 409 保护失效，导入快照可静默覆盖用户编辑（batchDelete 不递增无实际影响，deletedAt 兜底）。fin PRD §4 要求"对选择覆盖的每个项目以 dataRevision 为前置条件"。
- 修复方案：update/batchRestore 写语句递增 dataRevision（建议收敛到统一 `writeProjectChange` 入口防漏加）。
- **0.3 核实**：递增处实际为 **3 处**（描述称 2，漏 `profit.service.ts:248` cellSave，该处已正确递增）；保护失效链路确认（预览 dataRevision 与编辑后相同 → 条件更新通过 → 静默覆盖）。**实现注意**：若收敛到 `writeProjectChange`（审计写入口），须同时移除 detail/profit 两处显式递增防双加（每次 +2），更稳妥的收敛点是"项目行写入辅助函数"。

**✅ M16【FIN】导出缺失"序号"列**
- 位置：`apps/fin/src/modules/excel/export-builder.ts:127`（rowCells 首元素恒 null = 第 1 列）；模板首列表头为"序号"（xlsx-template.ts:35）
- 问题：PRD 要求"序号按导出结果重排"，数据行 A 列恒空（重新导入不受影响，序号列不在 IMPORTABLE_COLUMNS）。
- 修复方案：写数据行时按最终顺序填充序号（rowCells 增加序号入参）。

**✅ M17【FIN】确认接口幂等指纹不含文件内容**（0.3 复核：CONFIRMED，表述一处修正）
- 位置：`apps/fin/src/modules/excel/import.service.ts:250`（`fingerprint: fingerprintPayload({ choices })`，buffer 不参与）
- 问题：同幂等键 + 同 choices + 不同文件被静默重放首次结果，新文件从未被解析/写入，无提示。主 PRD §3.3"同一键对应不同指纹时返回 409"。
- 修复方案：指纹纳入文件内容（如 `fingerprintPayload({ choices, fileHash: sha256(buffer) })`），同文件重试仍重放。
- **0.3 核实**：影响本质成立（新文件内容被忽略、返回首次结果、无提示）；一处表述修正——"新文件从未被解析"不准确：重放检查在 `parseWithWorker`/`matchRows` 之后（import.service.ts:243-248），新文件仍会被完整解析与匹配，只是写入被跳过。**实现注意**：不要直接传 Buffer 入指纹（canonicalize 会展开索引属性，20 MiB 序列化开销大），先算 sha256 十六进制串再入指纹。

**✅ M18【FIN】确认写入逐行 N+1（违反集合式导入禁令）**（0.3 复核：CONFIRMED，建议需实现时注意；0.6 复核修复：matchRows 全空年度行时 `AND: []` 恒真致 findMany 全表读取——空键分支省略）
- 位置：`apps/fin/src/modules/excel/import.service.ts:619-663`（新增行 findFirst+create；覆盖行 findFirst+updateMany+deleteMany×3+rebuildDetails+snapshotDetails×2 ≈ 11 次往返/行；仅审计 createMany 批量）
- 问题：fin PRD §4（prd.md:97）显式禁止"查询一次、写入一次、审计一次"的 N+1，要求集合化。
- 修复方案：新增路径先一次 findMany 批量校验业务键再 createMany（P2002 兜底转 IMPORT_PREVIEW_STALE）；覆盖路径一次 findMany 校验存在性与 dataRevision，updateMany + count 核对；明细快照批量读取。
- **0.3 核实**：问题与 PRD prd.md:97 逐字吻合。**实现注意**：①覆盖路径每行携带各自 dataRevision，Prisma updateMany 单次仅一个 where——无法一次集合更新多行不同 dataRevision 条件，需保持逐行条件 updateMany（已满足核对语义）或改 raw SQL（`UPDATE ... FROM (VALUES ...)`）；②新增路径 createMany 抛 P2002 无法定位冲突行号（现有逐行 create 的错误消息带 rowNumber），批量化后需整体转 IMPORT_PREVIEW_STALE 或再查一次定位；③删除/重建/快照的批量合并见各文件行号，均无回归。

**✅ M19【FIN】导入并发占用在 Multer 缓冲完整请求体后才取得**
- 位置：`apps/fin/src/modules/excel/excel.controller.ts:33,50`（FileInterceptor）→ `import.service.ts:205,235`（acquireImportLock）
- 问题：Multer 缓冲先于锁获取、也先于权限认证（权限断言在 handler 内）；并发请求都会先把 20 MiB 读进内存才碰锁。fin PRD §4 要求"并发占用应在认证授权完成后、读取上传请求体前取得"。
- 修复方案：锁获取前置到 Multer 之前（守卫中完成认证 + acquireImportLock，release 挂 `res.on('close')`/异常 finally），handler 复用已持有锁。

### 前端（9 项，0.3 复核更正：原"8 项"计数错误）

**✅ M20【WEB】asset 四个"历史记录"独立权限无前端页面**（0.5 复核修复：借还历史页列契约对齐后端 `listHistory` 实际字段（recordType/userName/consumableName/qty/borrowedAt/dueAt/returnedQty/writtenOffQty）；`keyword` 筛选由死筛选转为真实能力——`BorrowHistoryQueryDto` 增补 keyword 字段、`buildBorrowWhereSql` 增加 ILIKE 条件（含单引号转义）；同步 `docs/api-documentations/asset.md` 与 OpenAPI 产物；0.6 复核修复：三个申请历史页与 mine 页复用 COMMON_COLUMNS 的 name 列恒空（ApprovalRequest 无 name）——新增 REQUEST_COLUMNS 对齐实际字段）
- 位置：后端端点已就绪——stock-in.controller.ts:45、stock-change.controller.ts:45、claim.controller.ts:45、borrow.controller.ts:54（`@Get('borrow-records')`）；权限目录 catalog.ts:189/207/225/237；前端 `AssetPage.tsx:80,82,84,301,328` 只接 `/mine` 变体，全 web 无非 mine 端点消费
- 问题：被授予"xx历史记录（部门/公司）"的用户无任何页面可用，授权完全失效。
- 修复方案：AssetPage NAVIGATION 增加四个菜单项（permission 用对应 history 功能码），以 ResourcePage 接入非 mine 端点；若 MVP 不落地则与 PRD/权限目录对齐说明。**需产品确认**。

**✅ M21【WEB】HR 审批中心导出必然 405**（0.6 复核确认：asset「导出已筛选」与「导出全部」共用 export/all 端点但前端携带筛选参数、后端 exportList 完整解释——筛选实际生效，无需修复）
- 位置：`apps/web/src/components/ApprovalCenter.tsx:97`（hr 用 POST）vs `apps/hr/src/modules/approval/approval.controller.ts:54`（`@Get('export')`）
- 问题：前端 POST vs 后端 GET，导出必然 405。
- 修复方案：对齐方法（建议后端改 `@Post` 与 platform/asset 一致，或前端按服务分派 GET/POST）。

**✅ M22【WEB】利润分析无"离开保护"**（0.5 复核修复：原修复的 `useBlocker` 在声明式 `<BrowserRouter>` 下必然抛错（react-router 7 源码确认：useBlocker 第一行 `useDataRouterContext` → invariant），打开 `/fin/profit` 即全应用白屏；已弃用 useBlocker，改为 location 变化检测 + antd Modal 确认 + `navigate(from, {replace:true})` 回退（restoreRef 防重入），beforeunload 保留；**0.6 复核修复：容器 switch 切换使组件卸载与导航同 commit、location 检测 effect 不再执行——站内保护为死代码，改为利润分析 display:none 常驻渲染；测试改真实容器结构并 portal mock 弹窗**）
- 位置：`apps/web/src/pages/fin/FinPage.tsx:175-266`；全 src 无 beforeunload/离开确认
- 问题：fin PRD §4 明文要求；未保存草稿刷新/切换即静默丢失（唯一涉数据丢失的前端缺陷）。
- 修复方案：存在草稿/保存失败时 `beforeunload` 提示 + 站内跳转 `useBlocker` 确认（"留在本页处理 / 放弃未保存内容并离开"）。
- **0.3 核实**：确认（SPA 内切换多被 blur 兜住，真实丢失窗口是刷新/关闭时焦点在编辑中、保存请求进行中或保存失败后）。**补充**：PRD :73 同源要求"保存中/已保存/保存失败"状态显示当前也缺失（`saveCell` 失败仅 toast、`moneyDrafts` 无持久状态标记）——离开保护缺少"保存失败"触发源，建议与 M22 一并落地。

**✅ M23【WEB】工程合同页违规提供导出入口**
- 位置：`FinPage.tsx:123` exportConfig 挂在 Projects 页；fin PRD §3（prd.md:63）明文禁止；导入导出在独立 `/fin/excel` 页（FinPage.tsx:268-341）
- 问题：导出入口放错页（PRD 仅利润分析页支持按模板导出）。
- 修复方案：删除 Projects 页 exportConfig；核对 finance_view 只读用户不可见导出；导入导出归属按 PRD 基线（利润分析页）**需产品确认**后迁移。

**✅ M24【WEB】门户待办角标单一总数渲染在每个系统卡片上**
- 位置：`apps/platform-core/src/modules/base/portal/portal.service.ts:74`（`badgeCount: localPending + remotePending`）、`PortalPage.tsx:97`（同一值渲染到所有卡片；:96 注释自认"联调后后端按系统返回"）
- 问题：base PRD §5 要求"系统入口展示当前用户的待处理数量角标（按对应审批功能的数据范围统计）"，应按系统拆分。
- 修复方案：后端按系统（BACKSTAGE/ASSET/HR/FIN）拆分返回 `badgeBySystem`，前端按 system.code 取对应值。

**✅ M25【WEB】固定资产台账对只读用户展示全部编辑入口**
- 位置：`AssetPage.tsx:146-188`（create/edit/batchDelete/rowActions 无条件渲染，无 can() 门控）；对比 FinPage 用 canMaintain 门控正确
- 问题：违背主 PRD §10.4"只读用户看不到任何编辑入口"。
- 修复方案：`const canMaintain = can('fixed_asset_maintain')`，对 create/edit/batchDelete/rowActions 及调度/报废按钮门控。

**✅ M26【WEB】品种/库位入口权限与权限目录不一致（asset_config vs inventory_manage）**
- 位置：`AssetPage.tsx:18-19`；后端 consumable.controller.ts:33,41,53,61 与 warehouse.controller.ts:29,40,52,60 均用 `ASSET_CONFIG_FUNCTION_CODE`；目录 catalog.ts:213-217 的 `inventory_manage` 说明含"品种、库位、库存、调拨与流水维护"
- 问题：前后端彼此一致但共同偏离 PRD/目录，持有 inventory_manage 的管理员无品种/库位界面入口。
- 修复方案：**需产品确认**归属后二选一——归 inventory_manage 则前后端同步迁移守卫；维持 asset_config 则修订权限目录描述与 PRD。

**✅ M27【WEB】个人中心缺"本人岗位申请记录"视图**（0.3 复核：CONFIRMED，HrPage 部分需修正；0.6 复核修复：记录抽屉状态筛选为死筛选（后端仅收分页）——移除筛选保留完整列表）
- 位置：`MePage.tsx:212-214`（仅提交申请与我的操作日志）；记录视图在 `HrPage.tsx:23,64-65`（菜单无 permission 字段、全员可见，挂在 platform 服务）
- 问题：base PRD §6"个人中心内可查看本人的岗位申请记录"为全员能力，无 hr 授权的员工无法查看自己的申请记录。
- 修复方案：MePage 增加"我的岗位申请记录"抽屉（复用 DataTable + `/me/position-applications`）；同时给 HrPage 岗位申请菜单补 permission 门控。
- **0.3 核实**：MePage 抽屉方案可行（`GET /me/position-applications` 已存在且按 applicantId 过滤）。**HrPage 部分需修正**：后端**不存在岗位申请管理员全量列表接口**（`internal-position-application.controller.ts:42` 仅支持本人查询）——HrPage"岗位申请"菜单实际展示查看者自己的申请记录，对管理菜单而言本身错位；仅补 permission 门控无法使其成为管理视图，应删除该菜单或新增管理员全量端点（需产品确认）。

**✅ M28【WEB】"注销借还处置"缺"处置记录"视图**（0.5 复核修复：PENDING/RECORDS 两视图列 key 对齐后端实际字段——PENDING 用 recordId/recordType/userName/consumableName/qty/dueAt（listPending 返回 snake_case 经 DataTable normalizeRow 转驼峰），RECORDS 用 userName/consumableName/disposalType/processorName/createdAt；原"目标用户/名称/状态"等列恒空问题消除）
- 位置：`AssetPage.tsx:94`（endpoint 写死 `?tab=PENDING`）；后端 disposal.controller.ts:24 支持 `tab === 'RECORDS'`
- 问题：asset PRD §9 要求"待处置/处置记录"两个视图，处置记录无入口。
- 修复方案：处置页加 PENDING/RECORDS 切换（Segmented），RECORDS 用同一 endpoint 传 `tab=RECORDS`。

### 部署（4 项）

**✅ M29【DEP】`RECOVERY_EXECUTOR_INTERNAL_BASE_URL` 多带 `/internal/v1` 后缀**
- 位置：`deploy/docker-compose.yml:148`；`apps/platform-core/src/modules/backstage/backup/recovery-session.client.ts:46`（`write('/recovery/session')`）；`packages/server/src/internal/internal-http.client.ts:86`（直拼）→ 请求 `.../internal/v1/recovery/session` → 404 → 恢复控制会话签发在生产必失败。对照 worker 的 `RECOVERY_EXECUTOR_URL` 不带后缀（正确）
- 问题：恢复人工介入通道（backstage PRD §10）在生产不可用。附带：recovery-session.client.ts:5 注释写 3010 与 compose 默认 3090 不一致。
- 修复方案：compose:148 去掉 `/internal/v1`；修正 client 注释；建议加拼接断言防回归。

**✅ M30【DEP】security-checklist "全部容器 cap_drop" 声明与实际不符**
- 位置：`deploy/docker-compose.yml:56-104`（postgres/redis 无 cap_drop/security_opt/read_only/tmpfs/user）；声明出处 compose 头注释 :6 与 `docs/security-checklist.md:39-40`
- 问题：§9.14 断言与实际不符，部署核对会给出错误结论。
- 修复方案：二选一——(a) 按 §9.14 补加固（postgres：`user: postgres` + `read_only` + tmpfs（/tmp、/run/postgresql）+ `cap_drop: [ALL]` + `cap_add: [CHOWN]`；redis 同理）；(b) 收敛文档口径为"后端与 web 容器加固，基础设施容器沿用上游镜像默认"并明示。推荐 (a) 并同步两处文档。

**✅ M31【DEP】release.sh 时钟检查只警告不失败**
- 位置：`deploy/release.sh:127-130`
- 问题：§9.14 要求"失败时阻止本次发布完成"；timedatectl 不存在时静默跳过。
- 修复方案：未同步升级为 `fail`（或加显式 `--allow-clock-drift` 放行开关）；timedatectl 缺失时打明确警告；security-checklist:43 措辞同步。

**✅ M32【DEP】恢复演练文档多处命令不可执行**（0.3 复核：PARTIAL——探针项已修，其余仍存在）
- 位置：`deploy/restore-drill.md:19-20`（错探针路径）、`:35-41`（`POST /internal/v1/backups/trigger` 路由不存在——全仓无该路由，备份触发仅会话保护的 `/api/v1/backups/immediate`）、`:66-71`（delivery URL 错带 `/internal/v1`（应为 `/recovery/delivery`）、body 含 JS 非法 `...` 占位、单引号内 `$(date +%s)` 不展开、缺必填 `backupId`）、`:74-88`（无恢复会话 Cookie 会 401）
- 问题：演练步骤按文档执行必失败。
- 修复方案：按真实路由重写可执行命令——备份触发改用管理后台页面（或 S4 新增的内部端点）；delivery 用 `http://127.0.0.1:3090/recovery/delivery` + 合法 body（补 backupId、去 `...`）；控制查询/retry 先经 `restores/session` 取 Cookie 或注明经浏览器页面操作。
- **0.3 核实**：`:19-20` 探针路径已被 S1 修复（现为 `/recovery/readyz`），该项从清单中移除；其余仍存在且需更新表述：`:35-41` 备份触发——S4 已新增 `/internal/v1/backups/immediate`（`@AllowedCallers('migration-runner')`），文档仍写不存在的 `/backups/trigger` 且 caller 用 `release-script`（不在白名单 → 403），命令仍不可执行；`:66-71` delivery 全部问题仍存在（`/internal/v1/recovery/delivery` → 404、`...` 语法错误、`$(date +%s)` 不展开、缺 `backupId`——恢复管道按 backupId 拉 OSS manifest 必失败）；`:74-88` status/retry 无会话 Cookie → 401，需先经 `/api/v1/restores/session`（超管）取 Cookie 或注明经浏览器页面操作。建议演练步骤 1 的备份触发改用管理后台页面并记录真实 backupId 供步骤 3 使用。

---

## 三、轻微问题（38 项）

### platform-core / worker / packages（6 项）

- **L1【PC】会话版本字段 pv/ov/otv/dv 占位注释过时**：`packages/server/src/session/session.service.ts:13,80-83`；四个字段从未被比较（守卫仅比较 sv/iat；授权每次请求实时读库）。修复：改写注释为"授权上下文实时读库，pv/ov/otv/dv 预留未参与校验"（或移除字段）。
- **L2【PC】M1 激活邀请生成无超管目标保护**：`admin-invitation.service.ts:30-40` 未检查 target.isSuperAdmin（对比 issueResetInvitation :92-98 有 FORBIDDEN 保护）。修复：与 reset 对齐，目标超管时校验操作人同为超管。
- **L3【PC】立即备份自动幂等键按分钟窗口**：`backup.service.ts:72-78`；"触发成功后异步任务失败"场景下同分钟重试被重放阻塞（触发事务回滚不留幂等记录不阻塞）。修复：自动键改为随机 UUID 或窗口内命中时返回当前同窗备份状态。
- **L4【PC】恢复执行器无 SIGTERM/SIGINT 处理**：`apps/recovery-executor/src/main.ts:17-23`（唯一无优雅停机的入口）。修复：参照 platform-core main.ts 接入 `beginShutdown()` + app.close() + 有界强退定时器，恢复各 stage 在 shutdown 标记后停止推进。
- **L5【PC】permission-catalog.module.ts:8 注释过时**（"权限组/守卫在后续任务加入本领域"，实际 T3-3/T3-4 已在同级 permission 模块完成）。修复：删除或改写注释。

### asset（5 项）

- **L6【AS】"快照形状兼容数组与单对象"未在借还历史/待处置 SQL 实现**：`borrow.service.ts:460,470,476,481`（`jsonb_array_elements` 遇单对象快照抛错、`@>` 数组匹配静默不匹配）、`disposal.service.ts:106-109`（listPending 直接 jsonb_array_length；同文件 listRecords :283-296 已用 CASE 守卫，两处口径不一致）。修复：统一改为 jsonb_typeof CASE 包裹 + EXISTS 兼容。
- **L7【AS】品种单位锁定检查遗漏待审批申领/变更明细**：`consumable.service.ts:448-459` hasBusinessFacts 未计 consumable_request_items/stock_change_items（同文件 countReferenced :468-489 已计入，口径不对称）。修复：hasBusinessFacts 增加两个 PENDING 子查询与 countReferenced 对齐。
- **L8【AS】库存流水操作人硬编码"审批系统"且 id 语义错误**：claim.service.ts:259、agent-claim.service.ts:191、stock-change.service.ts:173（用 `head.applicantId` 而非 processorId）、borrow.service.ts:219（applyReturnApproved 实传 applicantId）、stock-in.service.ts:149。修复：统一改真实处理人姓名；stock-change/borrow 处 id 修正为 processorId。
- **L9【AS】SCAN_CATALOG 二维码解析无权限校验**：`qr.service.ts:260-262` 无条件返回（ASSET/INVENTORY_ITEM 分支均有校验）。修复：至少校验登录用户持有任一资产功能，无权限与无效码同样返回 QR_INVALID 不泄露。
- **L10【AS】表设计文档缺 borrow_batch_allocations 登记**：`apps/asset/prisma/asset.prisma` 31 个 model vs `docs/database-design/asset.md` A-1~A-30（全文无该表）；该表被 claim.service.ts:287、borrow.service.ts:168-183 使用，是 A-23 核心配套表。修复：asset.md 增补登记（A-31 `borrow_batch_allocations` 借还批次分配明细，写明 1—N、issued/returned_qty 语义与"最后借出先归还"恢复规则），同步表间关系段。

### hr（6 项）

- **L11【HR】节假日复用缓存时 source 覆盖供应商标识**：`holiday.adapter.ts:73,101`（`{ ...cached, source: 'saved' }`），快照失去 'ailcc' 且无法与降级复用区分。修复：透传 `cached.source`；需"来自缓存"语义则加独立标志位。
- **L12【HR】加班取消路由未校验 requestType**：`overtime.controller.ts:59-63` 可取消任意 hr 类型待审批单（申请人有权取消，不构成越权但 API 职责错位）。修复：cancel 增加预期类型参数并在事务内断言 requestType='OVERTIME'。
- **L13【HR】部门/岗位删除预览统计口径不一致**：`department.service.ts:187-197`（按申请人部门快照）vs `position.service.ts:224-226`（按 targetPositionId）。两处维度不同但对各自删除场景语义均合理。修复：不改代码，在 delete-preview 文档/注释写明两处口径定义。
- **L14【HR】月份参数静默进位**：`packages/contracts/src/dto/overtime.dto.ts:25`（MONTH_PATTERN 只校验形状）；`2026-13` 0 校验错误、Date.UTC 进位到 2027-01。修复：MONTH_PATTERN 收紧为 `/^\d{4}-(0[1-9]|1[0-2])$/`，monthRange 对非法月份显式抛校验错误。
- **L15【HR】测试辅助 submitTestHeader 滞留生产服务类**：`hr-approval.service.ts:160-203` public 方法硬编码"占位部门/占位岗位"，仅注释声明禁止接入控制器，无运行时保护。修复：移入 spec 测试工具模块（或非 test 环境直接抛错）。
- **L16【HR】approval.controller.ts:88 注释"业务副作用 T5 no-op"过时**（T6 已接入副作用）。修复：删除或更新注释。

### fin（10 项）

- **L17【FIN】空年度新增行被标为 DUPLICATE 而非 YEAR_REQUIRED**：`import.service.ts:535-540` 三元分支使 YEAR_REQUIRED 落入 DUPLICATE。修复：ConflictItem.status 增加 'YEAR_REQUIRED' 枚举并单独映射。
- **L18【FIN】确认时不存在行号 choice 静默忽略 + OVERWRITE 落空转新增**：`import.service.ts:589-614`。修复：确认前遍历全部 choices 校验行号存在于重解析结果（否则 `IMPORT_CONFIRM_MISMATCH`）；createdRows 分支遇 decision='OVERWRITE' 抛 `IMPORT_PREVIEW_STALE`，禁止静默转新增。
- **L19【FIN】小计行写数值而非 SUM 公式**：`export-builder.ts:203-225`；往返不受影响（导入忽略小计行）。修复：5 个小计列写入 `=SUM(起:止)` 公式并保留数值缓存。
- **L20【FIN】导出成功日志在 res.end 后无条件写**：`export.service.ts:227-229`（断连也记成功）。修复：改 `res.on('finish')` 后写成功日志，中断记失败/不记成功。
- **L21【FIN】只读快照事务覆盖建簿全程且事务超时 130s > 总时限 120s**：`export.service.ts:89-223`。修复：工作簿构建移出事务（事务内完成读取/排序，提交后在事务外 worker 构建），缩短快照持有时间。
- **L22【FIN】导入文本单元格无长度校验**：`import-parser.ts:255-294`（页面 DTO 有 @MaxLength）。修复：validateProjectRow 按与 DTO 一致的长度常量校验（建议长度常量提到 contracts 共享）。
- **L23【FIN】分组行公式不受白名单校验**：`import-parser.ts:204-215`（仅数据行执行 validateProjectRow）。修复：分组行执行与数据行相同的公式白名单校验。注：全空行直接跳过、无实际影响，无需处理。
- **L24【FIN】只读用户导出操作日志功能编码固定 FINANCE_MAINTAIN**：`export.service.ts:288-295`。修复：按用户实际权限选 feature（view 用户记 finance_view）。
- **L25【FIN】非法导出 scope 复用导入错误码 IMPORT_SHEET_INVALID**：`excel.controller.ts:76-78`。修复：改抛 VALIDATION 类错误码或新增 EXPORT_SCOPE_INVALID（同步 contracts/OpenAPI）。
- **L26【FIN】确认请求未记录"覆盖警告确认"**：`packages/contracts/src/dto/fin.dto.ts:375-399`（ImportChoiceDto 无警告确认字段）。修复：增加 `confirmDataLossWarning: boolean`，服务端对 dataLossWarning 目标校验必为 true 并在审计中记录。

### 前端（6 项）

- **L27【WEB】登录页使用未定义 CSS 变量 var(--wbme-bg)**：`LoginPage.tsx:94`（styles.css 无定义）。修复：:root 定义该变量或改用 antd token。
- **L28【WEB】数字/金额筛选缺"不等于"操作符**：`DataTable.tsx:139-145`（后端 table-query.ts 已支持）。修复：OPERATOR_OPTIONS.number 增加 NOT_EQUALS 一项。
- **L29【WEB】金额/数字列未用等宽数字字体**：DataTable 无 tabular-nums。修复：数字/金额列渲染加 `fontVariantNumeric: 'tabular-nums'`。
- **L30【WEB】利润分析移动端未卡片化**：`FinPage.tsx:251-262` 裸 Table 横滑（DataTable 系页面均有卡片化）。修复：按断点渲染桌面表格/移动卡片两套视图（编辑态卡片需单独设计）。
- **L31【WEB】条件组下"所有（AND）"失效**：`DataTable.tsx:299-311`（有组时顶层恒 OR，AND 选择无效果）。修复：二选一——(a) 有条件组时隐藏/禁用 AND 并明示"组间取任一"；(b) 扩展前后端协议支持顶层 AND（同步 table-query 的 parse/compile/SQL 生成）。
- **L32【WEB】公告行内发布/撤回按钮无条件渲染**：`BackstagePage.tsx:531`（未按 row.status 门控）。修复：DRAFT 显示"发布"、PUBLISHING 显示"撤回"，其余不显示。

### 部署（5 项）

- **L33【DEP】日志驱动运行期核验缺 compress**：`release.sh:111-115`（inspect 只取 Type|max-size|max-file）。修复：format 追加 compress，断言改为 `^local|20m|5|true$`。
- **L34【DEP】nginx /internal/v1 被 SPA fallback 兜住返回 200**：`nginx.conf:88-96`（`location /api/` 不覆盖 /internal）；附带：backup.controller.ts:57 与 recovery-session.client.ts:15 注释声称"Nginx 同域代理 /recovery/*"，nginx.conf 无该配置，前端也无 /recovery 调用 → 恢复人工介入通道在生产链路不可达。修复：SPA fallback 前加 `location ~ ^/(internal|recovery)/ { return 404; }`；若恢复控制需浏览器直连则补 `/recovery/` 反代并保持 `/internal/` 404；同步修正注释与 checklist:38。
- **L35【DEP】release.sh 镜像构建两次且 tag 不一致**：`:62` build 打 :local、`:65` 迁移用 :local、`:68` up -d 用 :$TAG 触发再构建。修复：构建前统一 `export WBME_TAG="$TAG"`，三处同一 tag 单次构建。
- **L36【DEP】ci.yml 头注释与 dev-workflow §4 声明漂移**：ci.yml:5"E2E 待扩展"与同文件 e2e job 并存；dev-workflow 门禁清单缺 openapi verify/compose config/E2E。修复：更新 ci.yml 头注释；dev-workflow §4 补全现有门禁。
- **L37【DEP】健康状态页服务探针在生产无接线**：`health-status.service.ts:44-51` 拼 `${url}/readyz`（对恢复执行器同样缺 /recovery 前缀）；compose 未注入 `PLATFORM_CORE_HEALTH_URL`/`WORKER_HEALTH_URL`/`RECOVERY_EXECUTOR_HEALTH_URL`；worker 无 HTTP 监听（worker/src/main.ts:12）即便配置也 404。修复：compose 注入三个健康 URL（恢复执行器带 /recovery 前缀）；worker 补最小健康探针或健康状态页对 worker 走任务表汇总而非 HTTP 探测。

### E2E（1 项）

- **L38【WEB/E2E】E2E 8 个用例全部为读路径**：auth.spec.ts（3 例）、core-flow.spec.ts（3 例，文件头自注"读路径"）、portal.spec.ts（2 例）；无任何业务写链路（新建/提交/审批/删除均无），T10-4"核心业务链路"未达标。修复：补充至少一条写链路闭环（如新建资产→编辑→批量删除，或加班申请→取消、公告发布→撤回），含失败回滚断言；core-flow 增加超管身份显式断言（seed 已保证但无运行时校验）。

---

## 四、修复顺序建议

1. **第一批（发布/备份/恢复链路）**：S1 → S2 → S3 → S4、M29、M30、M31、M32、L33、L34、L35、L37
2. **第二批（业务硬伤）**：S5 → S6 → S7、M15、M16、M17、M18、M19、L17-L26
3. **第三批（安全与契约）**：S8、M1-M6、M7-M10、M13、M14、L1-L9
4. **第四批（前端功能）**：M20、M22-M28、L27-L32、L38
5. **第五批（跨服务与文档）**：M11、M12、L10-L16、L36
6. 涉及产品确认的项（M9、M20、M23、M26 及 L13 文档化）先与产品对齐口径，再按对应批次实现。
