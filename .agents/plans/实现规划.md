# WBME 实现规划

> 版本：0.2 · 2026-08-08 · 状态：待两人评审
> 基线文档：主 PRD `docs/prds/prd.md` 及各子系统 PRD、`docs/database-design/`（81 张表）、`docs/dev-workflow.md`（协作与 CI/CD）
> 本文档只规划**实现步骤与顺序**；各任务「具体做什么」以所标注的 PRD / 表设计章节为准，不在此重复功能细则。
> 全部架构决策已在 §6 确定。

---

## 0. 总览

- **目标**：按 PRD 实现 MVP 全功能——platform-core（base + backstage）、asset、hr、fin 四个后端部署单元、前端、Worker 与恢复执行器；ai / agent 本期不实现（主 PRD §1.3、fin PRD「本期无表模块」）。
- **推进原则**：契约先行、骨架先立、平台核心先行、业务模块按依赖打通后并行。
- **数据库**：81 张表设计已评审定稿（见 `docs/database-design/`），实现阶段直接落 Prisma 模型与迁移。
- **协作**：GitHub 单仓库 monorepo、直接推 main + CI 门禁、版本 = 发布 tag（见 `docs/dev-workflow.md`）。

## 1. 模块与部署单元

| 模块 | 部署单元 | schema | 表数 | PRD | 依赖 |
| --- | --- | --- | --- | --- | --- |
| base | platform-core | base | 5 | `docs/prds/base/prd.md` | 无（认证与门户） |
| backstage | platform-core | backstage | 19 | `docs/prds/backstage/prd.md` | base |
| asset | asset | asset | 30 | `docs/prds/asset/prd.md` | 权限、审批契约、日志、任务、文件/导出 |
| hr | hr | hr | 18 | `docs/prds/hr/prd.md` | 权限、审批契约、日志、任务、导出 |
| fin | fin | fin | 9 | `docs/prds/fin/prd.md` | 权限、日志、任务、导出 |
| 前端 | — | — | — | 主 PRD §10 + 各模块 | 全部后端 API 契约 |
| Worker / 恢复执行器 / Migration Runner | 基础设施 | — | — | 主 PRD §9.1/§9.9、backstage PRD §10 | — |
| ai / agent | 本期不实现 | — | — | `ai/prd.md`、`agent/prd.md` | — |

## 2. 依赖关系总图

```
阶段 0 工程基座（monorepo + 骨架 + 请求链路 + 内部 REST + 健康探针 + compose + CI + 前端骨架）
   │
   ▼
阶段 1 数据库建模（4 套 Prisma schema + 迁移 + 只读视图 + 种子）
   │
   ▼
阶段 2 base 认证链路 ──► 阶段 3 backstage 权限体系 ──► 阶段 4 平台基础设施
                                    │                        │
                                    ▼                        ▼
                             阶段 5 统一审批契约（共享内核）
                                    │
              ┌─────────────────────┼─────────────────────┐
              ▼                     ▼                     ▼
        阶段 6 hr 服务         阶段 7 asset 服务      阶段 8 fin 服务
              │                     │                     │
              └─────────────────────┼─────────────────────┘
                                    │
                    阶段 9 前端（T9-1/T9-2 可自 T0 后并行；
                    各系统页面随对应后端检查点推进）
                                    │
                                    ▼
                        阶段 10 部署、发布脚本、E2E 收尾
```

- 阶段 6/7/8 **可并行**（审批契约与权限体系定型后互不依赖）。
- 前端：**T9-1/T9-2** 可在阶段 0 骨架与契约包就绪后启动；**T9-3～T9-7** 随对应后端检查点并行，不必等 6/7/8 全部完成。
- 必须串行：阶段 0→1→2→3→5（权限体系与审批契约是全项目共享地基）；跨服务调用依赖 T0-8。

---

## 3. 阶段与任务清单

> 任务编号：`T{阶段}-{序号}`。每个任务标注对应 PRD 章节，**实现范围与验收细则以该章节为准**；下列验收句只写阶段级「可运行/可联调」共性要求（操作日志、错误码、API 文档随任务同步，不单独列任务）。

### 通用任务约定（所有任务适用，不逐条重复）

- **接口契约**：每个新增或变更的接口，其 DTO 校验测试与 OpenAPI 注释随代码同一次提交（主 PRD §9.5）。
- **API 文档**：`docs/api-documentations/` 中的接口文档随接口提交同步更新；**文档与实现不一致视为任务未完成**（文档在 T0–T9 各任务中随提交维护，T10-6 只做收尾核对审计，不在任务中补写）。
- **契约变更**：字段、错误码、权限要求、幂等语义等接口契约变更，提交前同步搭档（见 `docs/dev-workflow.md` §2 高冲突区约定）。
- **仓库文档维护**：每个任务完成后检查并更新仓库内相关文档——对应 PRD 是否需同步、`docs/database-design/` 表结构、`docs/directory.md` 目录登记、`docs/api-documentations/` 接口文档；文档与代码同一次提交。

### 阶段 0：工程基座（无 PRD 直接对应，支撑全项目）

**目标**：monorepo 骨架可跑、CI 转绿、开发环境可一键启动。

- [x] **T0-1 monorepo 初始化**：pnpm workspaces、根 `package.json`（聚合脚本 lint/typecheck/test/build，供 CI）、根 `.nvmrc`（Node 24 LTS）、统一 tsconfig 基座、目录结构（`apps/platform-core|asset|hr|fin|web|worker`、`packages/contracts|approval|logging|tasks|files`，共享包清单见 §6-2）、`.env.example`（机密仅占位，主 PRD §9.14）。验收：`pnpm install`（镜像源）后聚合脚本可执行。
- [x] **T0-2 共享包骨架**：`@wbme/contracts`（主 PRD §9.6 错误目录 MVP 业务域 + `BusinessException` 基类）、DTO 基类、枚举/字符串联合类型（与 Prisma enum 对齐）；时区与金额传输约定入口（主 PRD §9.10/§9.11）；`@wbme/approval` / `@wbme/logging` / `@wbme/tasks` / `@wbme/files` 本阶段建包结构与导出约定，实现随后续任务落地。验收：错误码域内唯一、HTTP 状态映射存在（测试校验）。
- [x] **T0-3 NestJS 应用骨架 ×4**：platform-core / asset / hr / fin 各自 NestJS 应用可启动（空模块 + 路由命名空间 `/api/v1`），Worker 与恢复执行器占位工程。验收：四服务 + worker 本地可启动。
- [x] **T0-4 统一请求处理链路**（主 PRD §9.6）：请求上下文、全局校验管道、认证/授权守卫挂载点、单一全局异常过滤器、访问结果/超时拦截器挂载点。验收：统一错误结构测试通过。
- [x] **T0-5 健康探针 + Redis 启动/降级挂载点**（主 PRD §9.8、§13）：各部署单元 `/healthz`、`/readyz`；Redis 命名空间约定与启动探测/就绪失败规则挂载；platform-core 就绪检查覆盖 base+backstage 依赖；探针免登录仅返回最小状态。验收：compose 内 healthcheck 可用。
- [x] **T0-6 本地开发环境启动脚本 + Migration Runner**（主 PRD §9.9）：开发环境不使用 Docker，依赖本机 PostgreSQL 与 Redis；`pnpm dev` 一条命令完成依赖可用性检查、执行 Migration Runner（按部署单元顺序执行迁移并校验 `_prisma_migrations` 隔离）、启动四服务 + Worker + web 骨架；迁移前自动创建「立即备份」的钩子（阶段 4 实现时接入）。验收：全新环境一条命令起全套。
- [x] **T0-7 CI 生效**（dev-workflow §4）：聚合脚本就位后 GitHub Actions 转绿；配置分支保护「Require status checks」。验收：push main 后 CI 全绿。
- [x] **T0-8 内部 REST 基础设施**（主 PRD §9.4）：各服务 `/internal/v1` 前缀、共享内部令牌校验、调用方服务白名单、超时与有界重试、`DEPENDENCY` 映射；Nginx/compose 不暴露该前缀到公网。验收：令牌错误/无权/依赖失败的错误类型符合 §9.5。
- [x] **T0-9 前端应用骨架**（主 PRD §10.1）：`apps/web` Vite + React + antd 空应用可构建与本地/compose 启动（主题与请求层完整实现见 T9-1）。验收：compose 内 web 容器可访问。

**检查点 0**：四服务 + web 骨架以本地进程启动（依赖本机 PostgreSQL/Redis）、CI 绿、两人均能 `pnpm dev` 跑通。

### 阶段 1：数据库建模（对应 `docs/database-design/` 全部 + 主 PRD §9.9）

**目标**：81 张表落地，4 套迁移序列与只读视图脚本就位，种子数据可初始化。

- [x] **T1-1 platform-core Prisma schema**：multi-file schema，`@@schema` 归属 base/backstage；base 5 张 + backstage 19 张（backstage 的 S-20 为引用 `base.user_table_prefs` 的说明，不建表）；枚举、CHECK、部分唯一索引按 `docs/database-design/base.md`、`backstage.md` 逐条落地。验收：`prisma migrate deploy` 在空库可执行；表设计文档与 schema 逐条核对无差异。
- [x] **T1-2 asset / hr / fin Prisma schema ×3**：各自部署单元 schema 与迁移历史（asset 30 / hr 18 / fin 9）。验收：同 T1-1。
- [x] **T1-3 迁移序列与 Runner 配置**（主 PRD §9.9）：四套迁移目录隔离、Migration Runner 按 `platform-core → asset/hr/fin` 顺序执行、启动时结构版本校验。验收：并发容器不互相干扰迁移。
- [x] **T1-4 只读视图脚本**（主 PRD §3.3/§9.9）：操作日志联合视图、hr 职称视图（hr PRD §8）、backstage 站点角色只读视图；Migration Runner 迁移后统一执行。验收：视图脚本可重复执行、字段与契约一致。
- [x] **T1-5 种子与初始化数据**：第一个超级管理员种子账号（base PRD §2、主 PRD §3.1）+ 权限目录初始注册（backstage PRD §1 及各子系统 PRD §1）。验收：初始化命令一次性展示激活链接，不写日志。

**检查点 1**：四库可迁移、视图可查、种子可建；`docs/database-design/` 与 schema 核对完成。

### 阶段 2：base 认证与账号（base PRD 全部 + 主 PRD §9.7/§9.8）

**目标**：认证与门户链路可用——全平台访问前提（细则见 base PRD）。

- [x] **T2-1 钉钉 OAuth 集成**（base PRD §2）。验收：扫码登录/注册/激活/重置流程可跑通，组织不匹配拒绝。
- [x] **T2-2 注册与激活**（base PRD §2）。验收：邀请重发即旧失效、双通道一次性、手机号唯一性部分索引生效。
- [x] **T2-3 密码登录 + 登录保护**（base PRD §2/§4）。验收：锁表、解锁后恢复、并发限流。
- [x] **T2-4 会话管理**（base PRD §3、主 PRD §9.8）。验收：改密后全部会话失效、撤权立即生效。
- [x] **T2-5 密码修改/重置**（base PRD §2、backstage PRD §3 联动）。验收：重置完成前旧会话有效、完成后统一失效。
- [x] **T2-6 统一门户**（base PRD §5；待办角标依赖各系统审批统计契约，阶段 5/6/7 联调）。验收：无权限系统入口不可见、公告状态正确。
- [x] **T2-7 个人中心**（base PRD §6；资料修改审批见 backstage PRD §5 / T5-3；岗位申请复用 **T6-6**；我的操作日志依赖 T4-1）。验收：资料审批通过才生效、岗位申请校验通过。

**检查点 2**：登录/激活/会话/门户端到端可用；安全事件全部落库。

### 阶段 3：backstage 权限体系（主 PRD §3.1 + backstage PRD §1/§3/§4/§6）

**目标**：功能权限与用户生命周期——全项目访问控制根基（细则见标注章节）。

- [x] **T3-1 权限目录与启动对账**（主 PRD §3.1、backstage PRD §1；目录定义含 asset/hr/fin PRD §1）。验收：目录漂移被对账修复、版本递增。
- [x] **T3-2 员工授权 CRUD**（主 PRD §3.1、backstage PRD §4）。验收：并发修改冲突保护、整批语义正确。
- [x] **T3-3 权限组**（主 PRD §3.1、backstage PRD §4）。验收：快照语义验证。
- [x] **T3-4 守卫与数据范围**（主 PRD §3.1、base PRD §3）。验收：越权返回 403/404、撤权即时生效；**提权（授权/站点角色提升）时接线 `SessionService.rotate` 旋转会话标识**（阶段2 机制已就绪，`session.service.ts:rotate` 待本任务接线，见 base PRD §3）。
- [x] **T3-5 用户管理**（backstage PRD §3；跨服务调用依赖 T0-8；生命周期任务消费见 T4-2 / T6-8）。验收：整批全有或全无、注销/恢复安全顺序正确。
- [x] **T3-6 超级管理员管理**（主 PRD §3.1）。验收：最后一名超管不可卸任。
- [x] **T3-7 系统与业务结构管理**（backstage PRD §6）。验收：入口可见性与状态联动正确。

**检查点 3**：权限目录对账、授权、守卫、注销恢复端到端可用；超管保护生效。

### 阶段 4：平台基础设施（主 PRD §3.3、§9.1–§9.3、§10.2/§10.3 + backstage PRD §7–§11）

**目标**：日志、任务、设置、公告、备份、恢复、健康、文件、通用导出、表格偏好——平台级能力齐备，业务模块随事务接入。

- [x] **T4-1 操作日志**（主 PRD §3.3）。验收：幂等重放不重复、业务回滚日志同步回滚；联合视图查询可用。
- [x] **T4-2 统一后台任务 + Outbox + Worker 处理器**（主 PRD §9.1）：任务事实表/Outbox/BullMQ/调度与租约；处理器覆盖 §9.1 列出的本期任务类型（定时/立即备份与保留清理、恢复请求投递、账号生命周期、未关联业务图片清理、待审批超时自动取消扫描）。验收：崩溃恢复不重复副作用、任务表为唯一事实来源；各类型至少有一条可测路径。
- [x] **T4-3 错误日志聚合**（backstage PRD §8）。验收：并发聚合不重复、终态不可回退。
- [x] **T4-4 安全日志**（backstage PRD §8）。验收：事件范围与 PRD 清单一致、敏感值不落库。
- [x] **T4-5 系统设置**（backstage PRD §7、主 PRD §1.4）。验收：改参数即时生效、快照规则不追溯。
- [x] **T4-6 更新日志与系统公告**（backstage PRD §9）。验收：并发发布不产生两条正在展示。
- [x] **T4-7 数据备份**（backstage PRD §10；对象上传依赖 T4-10）。验收：备份记录/清单/对象一致、清理不遗漏。
- [x] **T4-8 数据库恢复**（backstage PRD §10）。验收：恢复全流程维护状态正确、异常保持维护并人工介入。
- [x] **T4-9 健康状态页**（backstage PRD §11；含主 PRD §9.13 磁盘空间状态）。验收：汇总不展示任务明细。
- [x] **T4-10 统一文件存储**（主 PRD §9.2、`@wbme/files`）：图片与备份的 OSS 私有存储、预签名、校验/重编码与临时对象清理（清理任务接入 T4-2）。验收：临时对象可清理、正式关联后不被误删；备份前缀与图片前缀隔离。
- [x] **T4-11 通用导出能力**（主 PRD §10.3）：各服务复用的「导出所有/已筛选」、行数上限、一致性快照、单用户并发、120s 超时与基础工作簿样式；利润分析固定模板导出仍见 T8-5（fin PRD §4）。验收：超限整次拒绝、并发 429、超时受控取消。
- [x] **T4-12 用户表格偏好 API**（主 PRD §10.2；表设计 B-5 / A-30 / H-18 / F-9）：platform-core 内 base/backstage 共用 B-5，asset/hr/fin 各自同构表；筛选预设 + 列设置，账号维度读写。验收：跨设备可读同一预设；无越权读写他人偏好。

**检查点 4**：日志/任务/设置/备份/恢复/健康/文件/通用导出/表格偏好全部可用；业务模块可随事务接入。

### 阶段 5：统一审批契约（主 PRD §3.2）

**目标**：共享审批内核定型，hr/asset/backstage 审批头全部接入。

- [x] **T5-1 审批内核共享包**（主 PRD §3.2；超时扫描处理器接入 T4-2）。验收：并发处理仅一个成功、其余 CONFLICT。
- [x] **T5-2 审批中心 API 契约**（主 PRD §3.2）。验收：范围外记录不可见。
- [x] **T5-3 各模块审批头接入**（backstage PRD §5、hr PRD §4/§5、asset PRD §9）：资料修改、加班/岗位变更、asset 六类审批。验收：各模块条件唯一索引与待审批数量规则生效。

**检查点 5**：审批契约一处实现、多处生效；各业务待审批数量规则正确。

### 阶段 6：hr 服务（hr PRD 全部）

**目标**：按 hr PRD 实现人事系统后端全功能。

- [ ] **T6-1 组织架构**（hr PRD §5）。验收：删除/停用规则、版本递增、跨服务视图及时生效。
- [ ] **T6-2 岗位管理**（hr PRD §7）。验收：配置变更不制造非法组织关系。
- [ ] **T6-3 职称规则与视图**（hr PRD §8）。验收：组织/角色变更后视图立即反映。
- [ ] **T6-4 节假日适配器**（hr PRD §3）。验收：依赖失败降级、快照不可追溯改写。
- [ ] **T6-5 加班申请/代交/审批/历史**（hr PRD §3/§4；导出复用 T4-11）。验收：时间段重叠拒绝、批次原子性、汇总分钟精度。
- [ ] **T6-6 岗位申请**（hr PRD §5、base PRD §6）。验收：待审批期间被改为多部门则不可批准。
- [ ] **T6-7 人事配置与字典**（hr PRD §9）。验收：设置即时生效。
- [ ] **T6-8 恢复兼容性与账号生命周期内部接口**（backstage PRD §3、hr PRD §5；依赖 T0-8；消费 T4-2 生命周期任务）。验收：hr 停机时恢复功能不可用（DEPENDENCY）；注销后岗位申请按规则取消。

**检查点 6**：hr 全功能 + 跨服务接口（用户注销生命周期、恢复）联调通过。

### 阶段 7：asset 服务（asset PRD 全部）

**目标**：按 asset PRD 实现资产系统后端全功能。

- [ ] **T7-1 固定资产台账**（asset PRD §4；主图依赖 T4-10；导出复用 T4-11）。验收：状态机规则、调度记录强制。
- [ ] **T7-2 维修管理**（asset PRD §4）。验收：同一资产最多一张进行中维修单。
- [ ] **T7-3 消耗品配置**（asset PRD §5/§12；品种图片依赖 T4-10）。验收：品种删除限制、停用规则。
- [ ] **T7-4 库存核心**（asset PRD §5）。验收：账面/占用/可用一致、纠正不绕过审批。
- [ ] **T7-5 库存流水与调拨**（asset PRD §6；流水导出复用 T4-11）。验收：调拨总量不变、并发 CONFLICT。
- [ ] **T7-6 入库/库存变更申请**（asset PRD §6）。验收：占用/释放原子、入库仅增、变更仅扣减。
- [ ] **T7-7 消耗品申领**（asset PRD §5/§7）。验收：并发额度不足整单失败、代领不占个人额度。
- [ ] **T7-8 借还/归还/核销/结清**（asset PRD §8）。验收：占用转换原子、逾期只提示。
- [ ] **T7-9 注销员工直接处置**（asset PRD §8；非审批类型，见 asset PRD §9）。验收：与待审批占用互斥、恢复账号后拒绝。
- [ ] **T7-10 二维码**（asset PRD §11）。验收：作废不可恢复、无权限不泄露目标信息。
- [ ] **T7-11 资产配置**（asset PRD §12）。验收：重置日变更只影响新周期。

**检查点 7**：asset 全功能端到端（含 asset PRD §9 六类审批 + 注销借还处置）联调通过。

### 阶段 8：fin 服务（fin PRD 全部）

**目标**：按 fin PRD 实现财务系统后端全功能。

- [ ] **T8-1 项目主档**（fin PRD §3）。验收：业务键唯一、软删除占键。
- [ ] **T8-2 金额明细**（fin PRD §3/§5）。验收：删除审计原子、汇总实时计算。
- [ ] **T8-3 利润分析计算与即时保存**（fin PRD §4、主 PRD §9.11）。验收：并发同字段以最后提交为准、不同字段互不覆盖。
- [ ] **T8-4 Excel 导入**（fin PRD §4；CPU 工作池见主 PRD §9.1）。验收：超限/非法全部拒绝、预览过期 409、无部分写入。
- [ ] **T8-5 Excel 导出**（fin PRD §4；在 T4-11 通用规则之上使用固定模板 V2）。验收：导出可无歧义重新导入。
- [ ] **T8-6 项目操作记录**（fin PRD §5）。验收：与业务变更同事务。
- [ ] **T8-7 财务配置与地区字典**（fin PRD §6）。验收：字典删除引用拒绝、地区跨系统唯一维护点。

**检查点 8**：fin 全功能（含导入导出往返）联调通过。

### 阶段 9：前端（主 PRD §10 + 各模块 PRD）

**目标**：统一工程基座 + 各系统页面完整可用（桌面/移动端响应式）。页面清单与交互细则见各 PRD，不在此枚举字段级行为。

- [ ] **T9-1 前端工程基座**（主 PRD §10.1/§10.5；承接 T0-9）。验收：主题生效、错误统一反馈。
- [ ] **T9-2 通用表格体系**（主 PRD §10.2/§10.3；预设/列设置对接 T4-12；普通导出对接 T4-11）。验收：与后端筛选/排序/偏好契约联调。
- [ ] **T9-3 认证与门户**（base PRD §2/§5/§6）。验收：全流程可用。
- [ ] **T9-4 管理后台页面**（backstage PRD 全部：含用户管理与资料修改审批 §3/§5、权限、系统与业务结构、系统设置、操作日志、系统日志、更新日志与公告、数据备份、健康状态、系统首页/欢迎落地，主 PRD §2.1）。验收：危险操作二次确认、权限显隐正确。
- [ ] **T9-5 asset 页面**（asset PRD 全部：含 §10 系统首页、§9 六类审批与注销借还处置）。验收：核心流程桌面+移动可用。
- [ ] **T9-6 hr 页面**（hr PRD 全部；系统首页见主 PRD §2.1）。验收：代交、重叠提示、汇总下钻可用。
- [ ] **T9-7 fin 页面**（fin PRD §3/§4/§5/§6；系统首页见主 PRD §2.1）。验收：移动端不删减编辑能力。

**检查点 9**：前端全系统页面可用，E2E 用例可编写。

### 阶段 10：部署与收尾（主 PRD §9.12–§9.14、§13 + dev-workflow §3）

**目标**：生产环境可发布、发布流程自动化、全量测试收口。

- [ ] **T10-1 生产 Compose 与 Nginx**（主 PRD §9.12–§9.14；含优雅停机与资源边界）。验收：`docker compose config` 校验通过、日志驱动核验。
- [ ] **T10-2 发布脚本**（主 PRD §9.9、dev-workflow §3）。验收：失败即停、更新日志范围正确、重复发布不重复记录。
- [ ] **T10-3 恢复执行器部署**（backstage PRD §10）。验收：数据库损坏场景恢复演练一次。
- [ ] **T10-4 E2E 测试**（主 PRD 前端 + 核心业务链路）。验收：CI 或发布前置可运行。
- [ ] **T10-5 安全与合规收尾**（主 PRD §9.7/§9.14）。验收：对照清单逐项通过。
- [ ] **T10-6 API 文档审计**：OpenAPI ↔ `docs/api-documentations/` ↔ PRD/表设计三方一致性审计（只核对不补写）。验收：审计发现的不一致为 0。

**检查点 10**：生产环境从零到发布、恢复演练通过、E2E 全绿。

### 阶段 11：ai / agent 预留（ai PRD、agent PRD——本期不实现）

- [ ] **T11-1 架构边界预留**（主 PRD §13、ai PRD）。验收：本期无业务实现、不建表。

---

## 4. 并行与分工建议

| 组合 | 建议 |
| --- | --- |
| 主线（T0–T5 + T10） | 一人负责 platform-core 全链（工程基座、认证、权限、基础设施、审批契约、发布脚本） |
| 业务线 | 另一人从 T6（hr）或 T7（asset）入手（需等待 T3–T5 的共享包/契约接口稳定，可先并行开发 T6-1 组织、T6-4 节假日等相对独立部分） |
| 前端 | T9-1/T9-2 尽早并行；各系统页面随对应后端检查点推进；fin Editable Cells / Excel 交互建议后置到 T8 稳定后 |
| 文档同步 | 每个任务完成时同步检查 PRD/API 文档/表设计（项目宪法：代码修改须同步检查文档） |

## 5. 风险与缓解

| 风险 | 影响 | 缓解 |
| --- | --- | --- |
| 权限体系设计复杂度高（四版本失效、数据范围闭包、跨服务视图） | 延期/安全漏洞 | 阶段 3 独立成段、验收标准先行；共享包优先；跨服务契约（hr 视图、注销生命周期）在 T1-4 视图脚本阶段就定字段 |
| 数据库迁移并发冲突（两人同时改 schema） | 迁移序列混乱 | dev-workflow §2 高冲突区协调；各自本地库、迁移随代码走；Migration Runner 统一执行 |
| 利润分析 Excel 往返（模板/样式/上限/并发）是全项目最复杂的单点 | 返工 | T8-4/T8-5 预留完整阶段；模板与样式常量绑定版本；固定快照测试 |
| 恢复流程（维护状态/停写/正向迁移）无法自动化测试 | 生产事故 | T10-3 部署后强制做一次恢复演练 |
| 前端契约漂移（多个后端并行） | 联调返工 | 后端契约（DTO/错误码/OpenAPI）先行评审，前端只消费生成文档 |

## 6. 既定决策

1. **monorepo 工具**：纯 pnpm workspaces（根锁文件 + `pnpm -r` 聚合脚本），不引入 turborepo 等构建编排。
2. **共享包边界**：独立 `@wbme` workspace 共享包——`@wbme/contracts`（错误码/DTO/枚举/权限目录定义）、`@wbme/approval`（统一审批内核）、`@wbme/logging`（操作日志模板/错误日志/安全日志的预定义受限语句接口）、`@wbme/tasks`（统一任务表的受限创建与状态条件更新）、`@wbme/files`（OSS 预签名与图片处理约定，主 PRD §9.2）；asset/hr/fin 直接依赖，schema 归属约束不变（主 PRD §9.3/§9.1）。
3. **Node.js 版本**：24 LTS（Active LTS），写入根 `.nvmrc`，CI 与本地一致。
4. **前端推进策略**：与后端并行——后端 DTO/错误码/OpenAPI 契约先行；T9-1/T9-2 可自阶段 0 后启动，各系统前端随对应后端检查点推进。
5. **测试策略**：**全面单测**——Vitest 覆盖全部服务层逻辑（含错误目录、审批状态机、权限守卫、库存/额度一致性、Excel 模板校验）；UI 层以 Playwright E2E 为主；每个任务的验收均包含对应单测通过。
6. **文件存储**：按主 PRD §9.2 实现，任务落点为 **T4-10**（影响 T4-7 备份上传、T7-1 主图、T7-3 品种图片与临时对象清理）。
