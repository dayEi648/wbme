{
  "name": "@wbme/logging",
  "version": "0.1.0",
  "private": true,
  "description": "WBME 共享日志模块：操作日志模板、错误日志/安全日志预定义受限语句（主 PRD §3.3/§9.3）",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "lint": "eslint src --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "build": "tsc -p tsconfig.build.json"
  }
}
