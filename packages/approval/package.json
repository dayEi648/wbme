{
  "name": "@wbme/approval",
  "version": "0.1.0",
  "private": true,
  "description": "WBME 统一审批内核共享包（主 PRD §3.2，T5-1 实现）",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "lint": "eslint src --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "build": "tsc -p tsconfig.build.json"
  }
}
